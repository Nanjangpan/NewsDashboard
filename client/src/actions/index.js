import categoryActions from './categoryActions'
import backgroundActions from './backgroundActions'
import livedataActions from './livedataActions'
import alertActions from './alertActions'
import buttoncolorActions from './buttoncolorActions'

const allActions = {
    categoryActions,
    backgroundActions,
    livedataActions,
    alertActions,
    buttoncolorActions
}

export default allActions